I need to refactor the app: the app scenario will focus mostly for one-time visitor, allow them to practice immediately with a prepare suite (copy paste from some source). That's the value of the webapp: there will be someone prepare all the suites as copiable text. User just need to open the web and use direcly. 
They can choose the mode of practicing, but we should rewrtie the mode options as dropdown instead of select button like current.
They can also save the test to local to practice later (our collection feature) 
at homepage, there will be only one button "Luyện tập ngay". When pressing it will ask to input the suite as text. 
If local machine has saved suites, it show as list below the main button practice now.
The search input will allow to search full text (vn, pinyin and hanzi)
Translation type should only have: hanzi to pinyin, hanzi to vn, vn to hanzi. other drop. We dont have to include description for each option. make it a select dropdown for mobile
practice order will have random as default
Time will set limit for each word 15s as default.
The hamburger button in homepage now will have 2 options: create collection and setting. A collection must be easy to share, click share will show a bottom drawer of 2 option: copy to clipboard or download txt file. If copy, once done it should show a dialog guide how to import to new device ("Luyeejn tajp ngay" -> paste in where...)